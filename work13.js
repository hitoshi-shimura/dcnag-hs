let array = [0, 1, 2, 3, 4];
let len = array.length;
function calc_array_length(array_length) {
    let result = array_length*30;
    return result;
}
document.write(calc_array_length(len)); //引数に変数を格納